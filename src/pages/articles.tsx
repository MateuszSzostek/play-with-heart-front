import * as React from "react";
import type { HeadFC } from "gatsby";

const ArticlesPage: React.FC = () => {
  return <main>Aktualnosci</main>;
};

export default ArticlesPage;

export const Head: HeadFC = () => <title>Aktualnosci</title>;
