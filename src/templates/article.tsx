import * as React from "react";
import type { HeadFC } from "gatsby";

const ArticlePage: React.FC = () => {
  return <main>Aktualnosc</main>;
};

export default ArticlePage;

export const Head: HeadFC = () => <title>Aktualnosc</title>;
